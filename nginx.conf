
    server {

        listen 80;

        # Serve static files directly
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri.html $uri/ =404;

            #if 404
          }
    }

